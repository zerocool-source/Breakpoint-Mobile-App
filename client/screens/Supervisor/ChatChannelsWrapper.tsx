import React from 'react';
import ChatChannelsScreen from '@/screens/shared/ChatChannelsScreen';

export default function SupervisorChatChannelsWrapper() {
  return <ChatChannelsScreen role="supervisor" />;
}
